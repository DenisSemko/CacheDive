namespace ConfigAgent.API.Common;

public static class Constants
{
    public static class Exceptions
    {
        public const string ItemNotFound = "{Item} is not found";
        public const string ItemIsNullOrEmpty = "{Item} is null or empty";
    }
}